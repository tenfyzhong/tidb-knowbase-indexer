name: Sync Knowledge Base

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:
    inputs:
      force_clean:
        description: 'Clean run indicator'
        required: false
        type: boolean
        default: false
  push:
    branches:
      - main

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build TypeScript
        run: pnpm build

      - name: Run Knowledge Base Sync
        env:
          CONFIG_JSON: ${{ secrets.CONFIG_JSON }}
          TIDB_DATABASE_URL: ${{ secrets.TIDB_DATABASE_URL }}
          TIDB_HOST: ${{ secrets.TIDB_HOST }}
          TIDB_PORT: ${{ secrets.TIDB_PORT }}
          TIDB_USER: ${{ secrets.TIDB_USER }}
          TIDB_PASSWORD: ${{ secrets.TIDB_PASSWORD }}
          TIDB_DATABASE: ${{ secrets.TIDB_DATABASE }}
          GH_PAT: ${{ secrets.GH_PAT || secrets.GITHUB_TOKEN }}
          EMBEDDING_PROVIDER: ${{ secrets.EMBEDDING_PROVIDER || vars.EMBEDDING_PROVIDER }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN || secrets.CLOUDFLARE_API_KEY || secrets.EMBEDDING_API_KEY }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID || vars.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_BASE_URL: ${{ secrets.CLOUDFLARE_BASE_URL || vars.CLOUDFLARE_BASE_URL || secrets.EMBEDDING_BASE_URL || vars.EMBEDDING_BASE_URL }}
          CLOUDFLARE_MODEL: ${{ secrets.CLOUDFLARE_MODEL || vars.CLOUDFLARE_MODEL || secrets.EMBEDDING_MODEL || vars.EMBEDDING_MODEL }}
          EMBEDDING_DIMENSION: ${{ secrets.EMBEDDING_DIMENSION || vars.EMBEDDING_DIMENSION }}
          AUTO_EMBEDDING_MODEL: ${{ secrets.AUTO_EMBEDDING_MODEL || vars.AUTO_EMBEDDING_MODEL }}
          AUTO_EMBEDDING_DIMENSION: ${{ secrets.AUTO_EMBEDDING_DIMENSION || vars.AUTO_EMBEDDING_DIMENSION }}
        run: node dist/index.js
